# Codex Next Steps

This sequence starts only after this spec package is reviewed and approved.

## A. Produce Final Desktop Master Canvas

Create the `1440px` desktop master canvas from the locked mockup structure. Keep all coded content represented as safe-zone overlays, not baked art.

## B. Export Desktop Section Slices

Export desktop/tablet section slices from the master canvas. Confirm each adjacent slice shares exact seam geometry and includes appropriate bleed.

## C. Produce Final Mobile Master Canvas

Create a separate mobile master canvas at `390px` or `430px`. Redraw and simplify motifs for mobile instead of compressing desktop art.

## D. Export Mobile Section Slices

Export mobile-specific section slices with mobile-safe zones, taller stacked sections where needed, and matched mobile seam geometry.

## E. Build Static Preview Using Slices

Create a static preview that stacks desktop slices and mobile slices separately. Use real HTML placeholder content above the background art.

## F. Review Screenshots

Capture and review screenshots across desktop, tablet, and mobile breakpoints. Confirm seam alignment, safe-zone readability, and visual continuity.

## G. Shopify Temporary Test Page

Only after static preview approval, wire the approved slices into a temporary Shopify/Dawn proof page. This should still be a test page, not the real About page.

## H. Real About Integration

Only after Shopify proof approval, consider real About integration. That integration must be separately scoped and must not start from this spec task.

## Guardrails

- Do not touch active Shopify `/assets`, `/sections`, or `/templates`.
- Do not modify the real About page.
- Do not edit existing isolated packages.
- Do not create V5.5 files.
- Do not work on Belle Isle Outpost, JRS, #140-#151, `lwt-ds`, or launch waterfall tasks.
- Do not publish.
- Do not claim live-ready.
- Do not generate final theme files yet.
